# Avito Parser

Сервис на базе **Nest.js** и **Puppeteer**, который подключается к Личному кабинету Авито, отслеживает новые сообщения и транслирует их на фронтенд в реальном времени через **WebSocket**.

Проект реализован в рамках тестового задания на позицию **Nest.js разработчика**.

---

## 🧩 Архитектура

Проект состоит из двух частей:

- **Server** — Nest.js + Puppeteer + WebSocket  
- **Client** — простая веб-страница для отображения сообщений  

### Общий flow работы приложения

1. Nest.js запускает Puppeteer  
2. Puppeteer открывает браузер и инициирует процесс авторизации в Авито  
3. При необходимости пользователь вручную проходит SMS-подтверждение  
4. Сервис ожидает появления страницы мессенджера  
5. После подтверждения доступа:
   - подключается `MutationObserver`  
   - новые сообщения перехватываются  
   - сообщения транслируются клиенту через WebSocket  

---

## ⚠️ Ограничения (SMS)

Авито использует антибот-защиту в виде SMS-подтверждения.

❗ В рамках тестового задания **обход данных механизмов не реализован**.

### Как реализована авторизация

- браузер запускается в **не headless-режиме**  
- пользователь **вручную** проходит SMS-подтверждение  
- сервис **ожидает появления страницы мессенджера** как фактического подтверждения успешной авторизации  
- после этого начинается автоматическое отслеживание входящих сообщений  

Такой подход отражает реальные ограничения браузерной автоматизации и используется исключительно для демонстрации архитектурного решения.

---

## 🖥 Server (Nest.js)

### 📚 Стек технологий

Серверная часть проекта использует:

- Node.js  
- Nest.js  
- Puppeteer  
- WebSocket  
- TypeScript  

---

### ⚙️ Переменные окружения

Создайте файл `.env` в корне серверной части проекта и укажите необходимые параметры:

```env
AVITO_LOGIN=your_login
AVITO_PASSWORD=your_password
PUPPETEER_HEADLESS=false
AVITO_TARGET_USER=target_username
